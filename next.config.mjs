/** @type {import('next').NextConfig} */
const nextConfig = {
  basePath: '/hugogaray',
  output: 'export',  
  
  reactStrictMode: true,
  images: {
    unoptimized: true, 
  },

  
};

export default nextConfig;
